  
import * as React from 'react';
import { View, StyleSheet, Button, TouchableOpacity, Text } from 'react-native';
import * as Speech from 'expo-speech';
 
export default function App() {
  const speak = (word) => {
    Speech.speak(word);
  };
 
  return (
    <View style={{flex: 1, flexDirection: 'row', alignItems: 'center', flexWrap: 'wrap'}}>
      <TouchableOpacity onPress={()=>speak('elephants.')}>
        <Text style={{fontSize:100}}> 🐘 </Text>
      </TouchableOpacity>[
 

  <TouchableOpacity onPress={()=>speak('horse.')}>
        <Text style={{fontSize:100}}> 🐎 </Text>
      </TouchableOpacity>[
 

  <TouchableOpacity onPress={()=>speak('cow.')}>
        <Text style={{fontSize:100}}> 🐄 </Text>
      </TouchableOpacity>[
 


  <TouchableOpacity onPress={()=>speak('pig.')}>
        <Text style={{fontSize:100}}> 🐖  </Text>
      </TouchableOpacity>[
 


  <TouchableOpacity onPress={()=>speak('chicken.')}>
        <Text style={{fontSize:100}}> 🐓  </Text>
      </TouchableOpacity>[
 

  <TouchableOpacity onPress={()=>speak('dolphin.')}>
        <Text style={{fontSize:100}}> 🐬 </Text>
      </TouchableOpacity>[
 
    </View>
  );
}
 
